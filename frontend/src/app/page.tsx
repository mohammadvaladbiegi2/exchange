
import { Button, Typography } from '@mui/material';

export default function HomePage() {
  return (
    <div style={{ padding: '20px' }}>
      <Typography variant="h4" gutterBottom>
        Exchange mamad welcom
      </Typography>
      <Button variant="contained" color="primary">
        Buy bitcoin
      </Button>
    </div>
  );
}
